require 'spec_helper'

describe Predictor do
  it 'has a version number' do
    expect(Predictor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
